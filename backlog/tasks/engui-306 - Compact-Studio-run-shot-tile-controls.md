# ENGUI-306 - Compact Studio run shot tile controls

Status: done

## Request
Make the desktop Studio run shot tiles more compact:

- Move shot status onto the image area at the bottom-left.
- Move `v1 · unreviewed` review metadata onto the image area at the bottom-right.
- Replace the text `Change pose` button with an icon-only human + circular arrow button.
- Keep the scope desktop-only.

## Rollback
Revert the implementation commit and restart `engui-studio.service`.
